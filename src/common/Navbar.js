import React from "react";
import styled from "styled-components";

const NameTitle = styled.h1`
  font-size: 100px;
`;

export const Navbar = () => (
  <div>
    <NameTitle>Freddie Hoy</NameTitle>
  </div>
);
